//the shift function is similar in nature to the pop function. 
//however, the shift function removes the FIRST item in the array instead of the last item in the array. 
//this can be used in a queue data structure

var MyArray = [1,2,3,4,5];
MyArray.shift();

console.log(MyArray);


//the unshift function is similar to the push function 
//however, the unshift function is used to add an item to the front of an array
//for example, if we want to add 1 to the beginning of an array of numbers, we do the following

var MyArray = [2,3];
MyArray.unshift(1);

console.log(MyArray);

